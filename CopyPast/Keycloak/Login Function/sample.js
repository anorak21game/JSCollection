// ShifterNetwork.class
// 468

// session.login('messages');

// KeycloakSessionManager.class
// 155

// login(page) {
//     if (page == undefined) {
//         page = "login";
//     }
//     const url = this._private.keycloak.createLoginUrl({ 'redirectUri': this.IOR.loader.baseUrl + this.IOR.loader.basePath + '/src/html/KeycloakResponsePageLogin.html' + '?callFrom=' + page });
// }